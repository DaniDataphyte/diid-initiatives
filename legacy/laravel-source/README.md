# Legacy Laravel Source

This directory preserves the original Laravel implementation used during migration.

It is retained only as a source archive and should not be treated as the active runtime for production deployment.
